1.目标：完成核心解耦。
React + Tauri + Capacitor 移植计划


新增 services/ 目录。
所有页面不再直接访问 database/operations.py。
Worker 不再承担业务，只负责执行任务。
所有业务数据统一用 dataclass（或 Pydantic 模型）表示。
配置、数据库、AI 调用都通过 Service 层访问。

完成后，PySide6 功能应与现在完全一致，但内部结构更利于迁移。

2.（搭建新前端）
创建 React + Tauri 工程。
实现一个最简单的页面（例如 LearnPage）。
能打开本地数据库。
能显示字幕列表。
暂时不接 AI。

3.
接入 AI 调用。
完成学习页面的主要流程。
验证数据库读写和状态更新。

4.
完成 Review、收藏、表达库等页面。
开始测试 Android（Capacitor）适配。

